
<% @posts.each do |post| %>
  <tr>
    <td><%= post.content %></td>
    <td>→ID<%= post.id %></td>
    <td>
      <%= link_to 'show', post_path(post) %></br>
    </td>
  </tr>
<% end %>
<p>
    <%= link_to 'new', new_post_path %>
</p>
